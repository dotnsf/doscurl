/*
 * DOSCurl Configuration File for mTCP
 */

#ifndef CONFIG_H
#define CONFIG_H

#define MTCP_PROGRAM_NAME "doscurl"

// Disable heap checking BEFORE including GLOBAL.CFG
// This prevents false positives with large global arrays
#define UTILS_CHECK_HEAP (0)

// Include global mTCP configuration
#include "GLOBAL.CFG"

// Local TCP/IP library options
#define COMPILE_ARP
#define IP_FRAGMENTS_ON
#define COMPILE_UDP
#define COMPILE_TCP
#define COMPILE_DNS
#define COMPILE_ICMP

// Override defaults for our application
#undef TCP_MAX_SOCKETS
#define TCP_MAX_SOCKETS (4)

#undef PACKET_BUFFERS
#define PACKET_BUFFERS (20)

// Application specific settings
#define DOSCURL_VERSION "0.1.0"
#define DOSCURL_USER_AGENT "DOSCurl/0.1.0 (DOS; mTCP)"

#endif
